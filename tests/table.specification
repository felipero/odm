import net.fratech.easyd.BasicDAO

description "Table"

before "setting the table in the database",{
}

description "columns method"

it "should return the right columns", {
    "a".shouldNotBe null
    "foo".shouldBeEqual "foo"
}
